git:
    - create new repo

bootloader:
    - long mode supprt
    - second stage
    - filesystem support
    - reading kernel as a file
    - setting graphics mode
    - muliboot options
    - graphical os selecting interface
    - giving font array from file to kernel

kernel:
    - determine heap size by total memory size
    - page/swap file

makefile:
    - auto detect object files
    - make copy_data.sh script inline

shell:

libc:

desktop_env:

general: